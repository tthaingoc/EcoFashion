Xin xem giúp mình file FE-Implement-Wallet-Cart-Orders-Payment-Shipment.txt. Đây như 1 instruction những gì mình đã implement được. Hiện tại front end đã cho phép user nạp tiền vào ví thành công. Hỗ trợ xem giúp mình vì sao nút rút tiền không hoạt động được lại bị ẩn đi có logic gì liên quan
open the instruction file to understand the implementation scope. In parallel, I'll scan the frontend codebase for any references to wallet withdrawals to locate where the withdraw button and its visibility logic are handled.
wallet.ts
open the wallet mutation hooks to see if there is any logic that disables withdrawals, and confirm where the disabled state is enforced in the UI.
useWalletMutation.ts
đã đọc file hướng dẫn và rà toàn bộ code liên quan đến ví/withdraw trên frontend.
Nguyên nhân nút rút tiền không bấm được là do bị “disable” cứng trong UI, không phụ thuộc số dư hay quyền. Không có logic ẩn/hiện theo điều kiện; nó bị make mờ bằng class và thuộc tính disabled.
Các vị trí cụ thể:
WalletDashboardTailwind.tsx
          <button
            onClick={() => setShowDepositModal(true)}
            className="flex items-center gap-2 px-6 py-3 bg-white/20 hover:bg-white/30 rounded-lg transition-colors"
          >
            <PlusIcon className="w-5 h-5" />
            Nạp tiền
          </button>
          <button
            disabled
            className="flex items-center gap-2 px-6 py-3 bg-white/10 rounded-lg opacity-50 cursor-not-allowed"
          >
            <MinusIcon className="w-5 h-5" />
            Rút tiền (Sắp có)
          </button>
WalletDashboard.tsx
                <Button
                  variant="outlined"
                  size="large"
                  startIcon={<Remove />}
                  sx={{
                    color: 'white',
                    borderColor: 'rgba(255,255,255,0.5)',
                    '&:hover': { bgcolor: 'rgba(255,255,255,0.1)', borderColor: 'white' }
                  }}
                  disabled // We'll implement this later if needed
                >
                  Rút tiền
                </Button>
Hooks và API cho rút tiền đã có sẵn nhưng chưa được gắn vào UI
useWalletMutation.ts
export const useRequestWithdraw = () => {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: (payload: WithdrawRequest) => walletService.requestWithdraw(payload),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: QUERY_KEYS.wallet });
      queryClient.invalidateQueries({ queryKey: QUERY_KEYS.walletSummary });
      toast.success('Yêu cầu rút tiền đã được gửi thành công');
    },
    onError: (error: any) => {
      toast.error(error?.response?.data?.message || 'Lỗi khi tạo yêu cầu rút tiền');
    },
  });
};
Config ràng buộc rút tiền cũng đã có (min/max), nhưng UI chưa dùng để kiểm tra
wallet.ts
export const WALLET_CFG = {
  minDeposit: 10_000,
  maxDeposit: 50_000_000,
  minWithdraw: 50_000,
  maxWithdraw: 10_000_000,
  quickAmounts: [50_000, 100_000, 200_000, 500_000, 1_000_000, 2_000_000],
  commissionRate: 0.1, // 10%
};
Kết luận
Không có logic nào khác đang “ẩn” nút; nút rút tiền bị vô hiệu hóa cứng trong cả phiên bản MUI và Tailwind.
API và hook có sẵn, chỉ thiếu UI (modal form) + ràng buộc min/max + gọi useRequestWithdraw.
Nếu bạn muốn mình bật rút tiền luôn: mình sẽ thêm modal nhập số tiền + mô tả, validate theo WALLET_CFG và số dư, rồi gọi useRequestWithdraw. Bạn cần mình triển khai luôn không?

